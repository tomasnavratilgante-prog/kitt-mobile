import {Linking} from 'react-native'; export class NavService{async setDestination(dest){const url=`https://mapy.cz/zakladni?q=${encodeURIComponent(dest)}`; try{ await Linking.openURL(url);}catch(e){} const km=Math.max(5,Math.round(Math.random()*60)); const min=Math.round(km*1.05); return {distanceKm:km,etaMin:min}; } onProgress(cb){const id=setInterval(()=>cb({kmToNext:Math.random()*3}),9000); return ()=>clearInterval(id);} }